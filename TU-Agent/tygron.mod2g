use dummy as knowledge.
use tygron as actionspec.
use somegoal as goals.

exit=nogoals.

module helloWorld {
	% take down the buildings that we have just built
	if bel(buildings([building(Id, Name, List, Status)])) 
        then building_plan_demolish(Id) + delete(buildings([building(Id, Name, List, Status)])) + delete(havebuilding) + adopt(havebuilding).
	
	% find a function to build a student building
	 if bel(functions(FS), member([NAME,Id,L],FS), member('EDUCATION',L), land(1)) 
		then building_plan_construction(Id, 3, square(10,10,200,10)) + insert(built(1)).
		
	% find a piece of pre-determined ground and attempt to buy it	
	if true
		then map_buy_land(square(10,10,200,200), 500) + insert(land(1)).
		
	
}