use agentKnowledge as knowledge.
use tygron as actionspec.

order=linearall.

module demolish {
	% take down ewi
    if bel(buildings([building(Id, 'faculty elektrotechniek, wiskunde en informatica', List, Status)|X]))
        then building_plan_demolish(Id).
}