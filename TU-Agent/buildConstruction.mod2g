use agentKnowledge as knowledge.
use tygron as actionspec.

order=linearall.

module buildConstruction {
	
	if bel(functions(FS), member([Name,Id,L],FS), member('EDUCATION',L), demolishedPolygon(MP))
		then building_plan_construction(Id, 1, MP).
}