use dummy as knowledge.

% Define the building action
define	building_plan_construction(Id, Level, MultiPolygon) with
   pre{ true }
   post{ true }
  
% Define the buy land action
define map_buy_land(MultiPolygon, Double) with
	pre{ true }
	post{ true }
	
% Define the demolish building action
define    building_plan_demolish(BuildingId) with
  pre{ true }
  post{ true }